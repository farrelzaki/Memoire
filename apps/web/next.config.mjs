/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // When shared packages (packages/*) are imported, add them to
  // `transpilePackages` here, e.g. `transpilePackages: ['@memoire/ui']`.
};

export default nextConfig;
