[package]
name = "memoire-desktop"
version = "0.1.0"
description = "Tauri shell for Memoire — wraps the Next.js frontend, talks to the same NestJS API."
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
