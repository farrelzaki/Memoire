{
  "name": "@memoire/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell around the Memoire Next.js frontend (Sprint 12 — Desktop). Talks to the same NestJS API as the web app; no bundled backend.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/plugin-updater": "^2.0.0",
    "@tauri-apps/plugin-process": "^2.0.0"
  }
}
